<Page x:Class="BeautySalon.AppointmentPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BeautySalon"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AppointmentPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <ListView x:Name="AppointmentListView" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Disabled">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="3" Rows="3"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Padding="15" MinWidth="300" MinHeight="150" Background="{StaticResource MaterialDesignDarkSeparatorBackground}">
                        <Grid>
                            <TextBlock HorizontalAlignment="Right" Text="{Binding date, StringFormat='{}{0:dd.MM.yyyy}'}" FontWeight="SemiBold"/>
                            <StackPanel>
                                <TextBlock FontWeight="SemiBold" Text="{Binding id, StringFormat='Запись №{0}'}"/>
                                <TextBlock Text="{Binding Service1.name, StringFormat='Услуга: {0}'}"/>
                                <TextBlock>
                                    <Run Text="Цена:"/>
                                    <Run Text="{Binding Service1.price}" TextDecorations="Underline"/>
                                    <Run Text="₽" />
                                </TextBlock>
                                <TextBlock>
                                    <TextBlock.Text>
                                        <MultiBinding StringFormat="Мастер: {0} {1} {2}">
                                            <Binding Path="Employee1.lastName"/>
                                            <Binding Path="Employee1.firstName"/>
                                            <Binding Path="Employee1.fathersName"/>
                                        </MultiBinding>
                                    </TextBlock.Text>
                                </TextBlock>
                                <TextBlock>
                                    <TextBlock.Text>
                                        <MultiBinding StringFormat="Клиент: {0} {1} {2}">
                                            <Binding Path="Client1.lastName"/>
                                            <Binding Path="Client1.firstName"/>
                                            <Binding Path="Client1.fathersName"/>
                                        </MultiBinding>
                                    </TextBlock.Text>
                                </TextBlock>
                                <TextBlock Text="{Binding date, StringFormat='Дата: {0:dd.MM.yyyy HH:mm}'}"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5">
            <Button x:Name="PreviousPageButton" Click="PreviousPageButton_Click" Margin="0,0,5,0" Content="Назад">
                <!--<materialDesign:PackIcon Kind="PreviousTitle"/>-->
            </Button>
            <TextBlock x:Name="PageStatusTextBlock" Foreground="White" FontSize="20" VerticalAlignment="Center" Margin="0,0,5,0">1/6</TextBlock>
            <Button x:Name="NextPageButton" Click="NextPageButton_Click" Content="Вперед">
                <!--<materialDesign:PackIcon Kind="NextTitle"/>-->
            </Button>
        </StackPanel>
        <StackPanel HorizontalAlignment="Right" Grid.Row="1" VerticalAlignment="Center" Margin="0,0,5,0">
            <Button Click="CreateButton_Click">
                <Button.Content>
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon VerticalAlignment="Center" Kind="Create"/>
                        <TextBlock>Создать новую запись</TextBlock>
                    </StackPanel>
                </Button.Content>
            </Button>
        </StackPanel>
    </Grid>
</Page>
